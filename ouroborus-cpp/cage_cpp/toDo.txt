
Estava checando el test del constructor del Automaton
Y EL metodo de prueva de creatNode AutomatonTest.cpp 
-----------------------------------------------------------------------
Falta escrivir todos los  metodos para cambiar la normalizacion
de la topologia, y para seleccionar un tipo de vecindario especifico
y claro!! escrivir o poner en los matodos ya empezados las pruevas 
---------------------------------------------------------------------
En este cacho, toplogy tiene que meter objetos dimension como pointer 
al array no??
para que queremos un vector nuevo en topology????
/*Automaton 107*/
topology = new Topology(dimension);
           for(int i=0;i<dimension;++i){
               topology->setDimensionSize(i, Dsizes[i]);
           }
-----------------------------------------------------------------------
por no apuntar por pendejo tendre que trabajar mas, pero ni pedo, el
Automaton.h esta todo poco profaileado, se !tiene que checar que
 trampa!
con tanto container y tan pocos pointers

checa createNode() en particular
------------------------------------------------------------------------
Automaton.h 114
La creacion del vecindario en este momento solo crea TowwerNeighboorhod
pero cuando se tengan mas vecindarios habra que modificar la creacion
incluso el constructor del Automaton.h
-----------------------------------------------------------------------
vector <vector<int> > Map::setEdge(const vector<int> position){

Este metodo de map deve de cambiar para resivir un array de una
dimension, asi todo estara mucho mejor en automaton
----------------------------------------------------------------------

!!!!!importante!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
NO SE HAN hecho bien todo el viaje para el copy constructor en Autimaton
tengo que checar como esta el viaje de la inisialisacion y creacion
ed los elementos y de mas 

--------------------------------------------------------------- 

EN el automaton en:

template<class T> void Automaton<T>::createNode(){

para pasar a map->setEdge(position); y a vector< vector<int> > hood = map->getEdge();
necesitas crear un vector position, pero lo haces de un array
ya creado lo cual es una pendejada, asi que se tiene que corregir

----------------------------------------------------------------------