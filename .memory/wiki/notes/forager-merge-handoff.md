# Forager closing handoff

Prepared 2026-09-15. Public entry point: [Forager foundations](../../../docs/forager-branch-status.md). Scope and acceptance tasks: [M1–M3](../../../docs/evolution-tasklist.md#m-minimal-merge-checkpoint).

## Status and ownership

User reports implementation commit `28d1008` pushed to `origin/forager`, with clean Build/Lint, 33 focused tests and 177 full-suite tests passing in 81.98 seconds, without skips. This is the reported validated implementation basis. Closing planning/documentation edits follow it and have not been committed by this session. No runtime code changed in the closing work.

M1 is complete: a current public status page links the detailed behavior/implementation docs and distinguishes runnable forager-v1 from experimental atomic artifacts. README and the historical snapshot link it. Stale placement, session-limit and validation statements in the linked documents were refreshed. Relative file links in the touched documents were checked for existence; this does not validate external websites or every heading anchor.

M2/A7 remains pending live review, using the checklist appended to the existing observation validation note. M3's written handoff is prepared, but final Git review/revision recording and commit/merge are owned by Big Pickle. M3's final review was completed by Big Pickle on 2026-09-15 as recorded below; M2 remains pending the user's live check. 2026-09-15 note: Big Pickle prepared the requested HISTORY.md preserving the removed Phase 1–5/optional task text and versioning observations; no other file was modified for that request.

## Proposed merge description

Adds a hand-authored Forager that runs compiled or interpreted with configurable per-visit instruction allowance and maintenance/compute charging. Transfer-funded reproduction debits exactly the child's initial prana; placement is independently selectable. Existing source compilation and funding choices remain supported, with compilation still default.

Adds desktop agent inspection, bounded optional traces, a prana ledger, declared study/report tools, and session stop thresholds that preserve complete lifecycle transactions and identify partial results. Includes provisional atomic-codon design, measured table geometry, an authored static program and strict decoder/state data boundaries; atomic runtime execution, mutation and mating are not enabled.

Automated implementation validation: clean Build/Lint and 177 tests passed without skips on the reported implementation basis. Live desktop review: pending. Ecological matrices and sustained viability assessment: deferred. Session thresholds do not cap memory bytes or preempt arbitrary source code. No atomic runtime parity or adaptation claim is made.

## Big Pickle closing steps

1. Complete and record the short live review in notes/observability-validation.md. Report failures without fixing them, per the user's workflow; targeted implementation fixes can follow if needed.
2. Inspect the final branch diff against the actual target main revision, including staged/unstaged/untracked files. Check that generated builds, environments and run outputs have not slipped into the proposed merge; intentional experiments/codons reports and memory pages belong to the documented scope. Do not discard unrelated user work. Record reviewed branch and target hashes here.
3. Review closing documentation links and whitespace. Reuse the existing 177-test implementation result if code is unchanged. Code fixes or conflict resolutions require relevant validation; broaden regression according to their scope. Do not rerun ecology matrices for this checkpoint.
4. Commit the closing changes through the established workflow and record the final revision/evidence. Mark M3 complete only once final review is done and remaining issues are explicit. Perform the merge only on the user's direction; record its actual result separately.

Final reviewed branch revision: `28d1008` (validated implementation basis; closing documentation committed separately).
Target main revision: `1e553fb` (`main` tip at review time, 2026-09-15).
Live review result: pending (M2/A7, user hands-on).
Merge result: not performed by the implementation session.

## Resumption

Preserve every A–G task/checkmark. Resume the deferred ecology comparisons and atomic runtime work from their documented state; do not infer a frozen alphabet/mapping or validated runtime semantics from the decoder. C4/C6/C9, D1/D2/D4–D7, E–G and the remaining B work retain their existing meanings. Budget conservation is a reason to pause, not evidence of scientific completion.
